<%= form_with model: @driver, class: 'driver-form' do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :vin %>
  <%= f.text_field :vin%>

  <%= f.submit "Create Driver", class: 'driver-form-submit-button' %>

<% end %>
